#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);cout.tie(NULL);

    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        string s;
        cin>>s;
        if(n&1)
        {
            for(int i=0;i<n;i++)
            {
                if(s[i]=='0'||s[n-i]==)
            }
        }
        else
        {
            if(s[0]&1 || s[1]&1)
                cout<<1<<"\n";
            else
                cout<<2<<"\n";
        }
    }
}
