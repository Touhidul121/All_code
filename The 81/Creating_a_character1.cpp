#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int str,intel,n,x,y;
    cin>>str>>intel>>n;
    if(str>intel){
    y=intel-diff;
    x=n-y;
    }

}
